# CNN kernel lab

Implement the two primitives underlying a convolutional block on a tiny deterministic image. The fixture distinguishes valid cross-correlation from a flipped mathematical convolution.

```sh
python3 test_public.py
STUDENT_MODULE=reference_solution python3 test_public.py
```

Complete TODOs in `starter.py` and preserve the function signatures.
