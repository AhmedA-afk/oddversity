# RNN sequence lab

Build the recurrence deliberately rather than treating hidden state as magic. The fixture is tiny enough to calculate by hand.

```sh
python3 test_public.py
STUDENT_MODULE=reference_solution python3 test_public.py
```

Implement `tanh` and `step` in your own `starter.py`.
