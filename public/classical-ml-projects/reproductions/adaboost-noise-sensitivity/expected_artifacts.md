# Expected artifacts

`artifacts/noise_sensitivity.json` must contain an entry for each requested noise
rate with `accuracy`, `final_weighted_error`, and `rounds_completed`. Keep the
noise generation seeded; test runs must be reproducible.
