# Expected artifacts

- `predictions.csv` containing all fixture ids.
- `metrics.json` containing `n` and `mae`; fixture MAE must be <= 16.
- For real data: chronological backtest table, naive baseline comparison, and a forecast-horizon statement.

