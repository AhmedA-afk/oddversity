# Expected artifacts

- A completed `starter.py` whose functions have no TODO exceptions.
- `run(fixture)` returning two probability rows and a finite scalar loss.
- A short note explaining why logits use stable softmax and why logits—not probabilities—are normally passed to a framework cross-entropy loss.
