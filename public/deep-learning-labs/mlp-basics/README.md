# MLP basics

Implement a deterministic two-layer computation contract using only the standard library. This trains shape, activation, stable softmax, and loss reasoning without hidden framework behavior.

## Run

```sh
python3 test_public.py
# Reference only:
STUDENT_MODULE=reference_solution python3 test_public.py
```

Do not copy the reference solution. Finish each TODO in `starter.py`, then record the probability normalization and loss calculation in your notes.
