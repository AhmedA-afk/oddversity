export interface QuizQuestion {
  prompt: string;
  options: string[];
  answer: number;
  /** The takeaway, shown whichever option was chosen. */
  explanation: string;
  /**
   * Per-option feedback, index-aligned with `options`. The correct entry says
   * why it holds; each distractor names the specific misconception behind it.
   * Diagnosing the wrong answer teaches more than scoring it.
   */
  why: string[];
  lesson?: string;
}

export interface QuizTrack {
  id: string;
  name: string;
  summary: string;
  questions: QuizQuestion[];
}

const q = (
  prompt: string,
  options: string[],
  answer: number,
  explanation: string,
  why: string[],
  lesson?: string,
): QuizQuestion => ({ prompt, options, answer, explanation, why, lesson });

export const quizTracks: QuizTrack[] = [
  {
    id: 'ai-foundations',
    name: 'AI foundations',
    summary: 'Check the mental models behind tokens, training, inference, and model choice.',
    questions: [
      q('What does an autoregressive language model predict during generation?', ['The next token given the preceding context', 'The user\'s identity', 'A database row chosen by ID', 'A fixed answer from training'], 0, 'Generation proceeds by repeatedly estimating a distribution over the next token, then appending a selected token.', ['Right. It estimates a distribution over the next token given everything before it, then appends one and repeats.', 'The model has no notion of who you are. Any apparent personalisation comes from what is in the context, not from identification.', 'There is no lookup table. Nothing is retrieved by key unless you built a retrieval step yourself.', 'Answers are generated fresh each time, not recalled. That is exactly why the same prompt can produce different text.']),
      q('Which statement best describes inference?', ['Updating model parameters with labeled examples', 'Using fixed parameters to produce an output for an input', 'Removing tokens from a context window', 'Converting text into HTML'], 1, 'Inference is the runtime use of a trained model. Prompting and retrieval change the input context, not the model parameters.', ['That describes training. Inference does not change any parameter.', 'Right. Fixed weights, an input, an output — everything you do at runtime changes the input, never the model.', 'Trimming context is something your application does before inference; it is not what inference means.', 'Formatting is unrelated. Inference is about running the model, whatever the output format.']),
      q('Why do tokens matter to an application?', ['They determine context, latency, and usage cost', 'They are always equal to one word', 'They guarantee factuality', 'They only matter during pretraining'], 0, 'Token count affects how much fits in context and how much work and billing a request can incur.', ['Right. Tokens are the unit for context limits, for latency, and for the bill.', 'A common assumption, and wrong in the places that hurt: rare words, names, code and non-Latin scripts split into several tokens.', 'Token count says nothing about truth. A long, well-tokenised answer can be entirely fabricated.', 'They matter at every call. Pretraining is where the tokenizer was fixed, not where its consequences end.'], '/learn/ai-foundations/tokens-context-cost'),
      q('What does temperature primarily change?', ['The model\'s learned knowledge', 'The sampling distribution over possible next tokens', 'The context-window limit', 'The tokenizer vocabulary'], 1, 'Temperature changes how concentrated or spread out sampling is. It does not add knowledge or repair missing evidence.', ['Turning temperature down does not add knowledge. A confidently wrong answer at 0 is still wrong.', 'Right. It reshapes the distribution the next token is sampled from — selection, not knowledge.', 'The context limit is a property of the model, unaffected by sampling settings.', 'The vocabulary is fixed at training time and has nothing to do with sampling.']),
      q('What is a context window?', ['A user\'s long-term memory store', 'The bounded token budget a model can process in a request', 'A tool permission list', 'A model training checkpoint'], 1, 'A context window bounds the input and generated output that can be processed together. It is not durable memory.', ['There is no durable memory across requests. Anything the model should remember must be sent again.', 'Right. A per-request budget covering input and generated output together.', 'Permissions live in your code. The context window is a size limit, not an authorisation mechanism.', 'A checkpoint is a saved set of weights. Unrelated to how much text fits in a request.']),
      q('When is a smaller model a sensible choice?', ['When it meets quality, safety, latency, and cost requirements', 'Whenever the task contains a proper noun', 'Only for image generation', 'Never, because larger is always better'], 0, 'Choose with an evaluation set and total system cost, including retries, retrieval, and review.', ['Right — and the way to know is an eval set plus the total system cost, including retries and review.', 'Nothing about proper nouns favours a smaller model. Task difficulty is what matters.', 'Smaller text models are widely useful; classification, extraction and routing are the classic cases.', 'Larger costs more and is slower. If a smaller model passes your eval, the larger one is buying nothing.']),
      q('What is an embedding?', ['A vector representation produced by a model', 'A hidden system prompt', 'A database backup', 'A temperature setting'], 0, 'Embeddings map content into vectors that can support similarity search. Similarity still needs task-specific evaluation.', ['Right. A vector whose position encodes meaning, which is what makes similarity search possible.', 'A system prompt is text. An embedding is a numeric representation, and the two are unrelated.', 'Nothing is being backed up. An embedding is derived from content, not a copy of it.', 'Temperature is a sampling parameter. Embeddings are produced by a different kind of model call entirely.']),
      q('Why can a fluent model answer still be wrong?', ['Fluency and truth are separate properties', 'The model always copies a source verbatim', 'Tokens cannot represent facts', 'Inference never uses context'], 0, 'The training objective rewards plausible continuations, not a proof that every claim is true.', ['Right. The objective rewards plausible continuation, and plausible is not the same as true.', 'It rarely copies verbatim. It produces what fits the pattern, which is why invented citations look so convincing.', 'Tokens represent text perfectly well. The gap is between plausible text and true claims.', 'Inference uses context heavily — that is the whole point of prompting and retrieval.']),
    ],
  },
  {
    id: 'prompt-engineering',
    name: 'Prompt engineering',
    summary: 'Practice the decisions that make prompts clearer, testable, and less fragile.',
    questions: [
      q('What should a reliable task prompt make explicit?', ['Task, context, constraints, and expected output', 'Only a friendly persona', 'The model provider\'s internal weights', 'A promise that the answer is correct'], 0, 'A prompt should define what to do and what success looks like. Validation and evaluation still belong around it.', ['Right. What to do, what it is about, what the limits are, and what the output should look like.', 'A persona changes tone and little else. It does not tell the model what success looks like.', 'Weights are not something a prompt can reach, and knowing them would not help you write one.', 'Asserting correctness in the prompt does not produce correctness. Validation happens outside the model.']),
      q('What is a good use for few-shot examples?', ['Showing a subtle format or classification boundary', 'Replacing all application validation', 'Guaranteeing no hallucinations', 'Increasing the context window'], 0, 'Examples are useful when the desired behavior is easier to demonstrate than to state as a rule.', ['Right. When the behaviour is easier to demonstrate than to describe, examples do the work a paragraph cannot.', 'Examples shape output; they do not check it. Validation still belongs in your code.', 'Nothing guarantees that. Examples reduce a class of formatting and boundary errors, not fabrication.', 'Examples consume context rather than expanding it — a real cost to weigh against the gain.']),
      q('What do delimiters primarily help with?', ['Making instruction and content boundaries legible', 'Encrypting untrusted text', 'Granting content higher authority', 'Reducing tokenization to zero'], 0, 'Delimiters reduce ambiguity for readers and models, but they are not a security boundary.', ['Right. They make the boundary between instruction and content legible to both readers and models.', 'Nothing is encrypted. The content is still fully visible to the model.', 'The opposite intent: delimiters mark content as data to analyse, not as instructions to obey.', 'Tokenisation still happens. Delimiters are a few extra tokens, not a way around them.']),
      q('Where should a durable application rule usually live?', ['In stable application instructions and enforced code', 'Only in a user message', 'Only inside retrieved documents', 'In the model temperature'], 0, 'Use system-level instructions for durable guidance, and enforce permissions and business rules outside the model.', ['Right. Durable guidance at the system level, and anything that must actually hold enforced in code.', 'A user message is per-turn and user-controlled — the weakest possible place for a durable rule.', 'Retrieved documents are untrusted input. A rule placed there can be overwritten by whoever writes the document.', 'Temperature affects sampling. It cannot express a rule of any kind.']),
      q('What is the strongest way to compare a prompt change?', ['Run old and new prompts on the same representative evaluation set', 'Ask one colleague which sounds nicer', 'Increase temperature and compare one sample', 'Change the model at the same time'], 0, 'A fixed test set makes wins, regressions, and attribution visible.', ['Right. Same inputs, both prompts, one number that moves — the only comparison that attributes the change.', 'One person\'s impression on one example is not evidence, and it is how regressions ship.', 'Raising temperature adds variance, which makes a single-sample comparison less informative, not more.', 'Changing two things at once means you cannot attribute the result to either.']),
      q('What should you request instead of private chain-of-thought by default?', ['A concise answer with assumptions or evidence', 'A hidden API key', 'A longer unverified paragraph', 'No output at all'], 0, 'A concise rationale or evidence summary is easier to audit and can be part of a clear output contract.', ['Right. A short rationale or the evidence used is auditable and can be part of the output contract.', 'Not something to request from a model, and not related to reasoning.', 'Length is not rigour. An unverified paragraph is harder to audit, not easier.', 'You still need an answer. The question is what accompanies it.']),
      q('What is a prompt injection?', ['Input that tries to change the model\'s intended behavior or instructions', 'A tokenizer compression method', 'A harmless spelling error', 'A vector distance metric'], 0, 'Treat user, retrieved, and web content as potentially adversarial and use layered controls.', ['Right — and note that the dangerous version arrives indirectly, in a document or tool result rather than from the user.', 'Unrelated. Compression concerns how text becomes tokens, not who controls behaviour.', 'A typo is noise. Injection is content deliberately shaped to redirect the system.', 'That is a retrieval concept. Injection is about instructions reaching the model, not about measuring similarity.']),
      q('What should happen after structured output is generated?', ['Parse and validate it before using it downstream', 'Trust it because it looks like JSON', 'Render it as executable code', 'Discard the schema'], 0, 'A schema helps the model produce a shape; a real parser and application checks enforce it.', ['Right. A schema constrains shape; a parser and your own checks are what actually enforce it.', 'Looking like JSON is not being valid JSON, and valid JSON can still carry a wrong value.', 'Rendering model output as executable code is how a formatting problem becomes a security incident.', 'The schema is what makes validation possible. Discarding it removes the check.']),
    ],
  },
  {
    id: 'rag',
    name: 'Retrieval-augmented generation',
    summary: 'Test your grasp of chunking, hybrid search, grounding, and RAG evaluation.',
    questions: [
      q('What does RAG add to a generation request?', ['Relevant external content retrieved for the request', 'A second training phase', 'A guarantee that every answer is true', 'A larger tokenizer'], 0, 'RAG retrieves context at request time. The system still needs to handle missed, stale, or hostile evidence.', ['Right. Relevant content fetched at request time and placed in the prompt.', 'No training happens. The model\'s parameters are untouched, which is precisely the appeal.', 'Nothing guarantees that. If retrieval returns the wrong passage, the answer is wrong with full confidence.', 'The tokenizer is unchanged. Retrieval adds content, not vocabulary.']),
      q('Why can lexical search complement semantic search?', ['It handles exact terms such as IDs and error codes well', 'It always understands paraphrases better', 'It removes the need for ranking', 'It prevents access-control errors'], 0, 'Hybrid retrieval combines different strengths. Exact identifiers are often a weakness of purely semantic search.', ['Right. Product codes, error strings and version numbers are exactly what dense vectors compress away.', 'That is the semantic side\'s strength. Lexical matching is weak on paraphrase.', 'It adds a second ranking to merge, so ranking becomes more important, not less.', 'Permissions are a filter you apply. No search method enforces access control by itself.']),
      q('What is a sensible chunking principle?', ['Preserve meaning while keeping passages focused enough to rank', 'Use one character per chunk', 'Always split at an arbitrary byte count', 'Put every document in one chunk'], 0, 'Structure-aware boundaries and measured overlap often beat a fixed rule applied to every document.', ['Right. Preserve enough meaning to answer, stay focused enough to rank.', 'A single character carries no meaning to embed. Retrieval would return noise.', 'Byte counts cut tables from their headers and steps from their procedures — the classic silent failure.', 'One giant chunk has a blurry average meaning and matches nothing specifically.']),
      q('What is a reranker used for?', ['Scoring retrieved candidates against the query more precisely', 'Generating database credentials', 'Expanding the context window', 'Replacing all ingestion'], 0, 'A reranker can refine a candidate set after initial lexical or semantic retrieval.', ['Right. A more expensive model scores the candidates the first pass found, and precision improves.', 'Nothing to do with credentials. A reranker only reorders results.', 'The window is fixed. Reranking helps you use it better by sending fewer, better passages.', 'Ingestion still has to happen. Reranking operates on what retrieval already returned.']),
      q('What does groundedness ask about an answer?', ['Whether its claims are supported by the supplied evidence', 'Whether it uses the highest temperature', 'Whether its font is readable', 'Whether the index has a logo'], 0, 'Groundedness is about evidence support. It is distinct from fluency and retrieval recall.', ['Right. Every claim traceable to a supplied passage — separate from whether the answer reads well.', 'Sampling settings have no bearing on whether a claim is supported.', 'Presentation is not evidence. A beautifully formatted answer can be entirely unsupported.', 'Branding has nothing to do with it. Groundedness is a property of the answer against its sources.']),
      q('How should a RAG system handle missing evidence?', ['Allow a clear abstention or not-found response', 'Invent a likely source', 'Return the oldest document', 'Increase temperature'], 0, 'A trustworthy system needs a path that says the available evidence is insufficient.', ['Right. An explicit not-found path is what lets you monitor a refusal rate instead of a silent fabrication.', 'Inventing a source is the single most damaging failure this kind of system has.', 'Age is not relevance. Returning something irrelevant because it exists is worse than returning nothing.', 'Sampling settings cannot conjure evidence that was never retrieved.']),
      q('What should document ingestion preserve?', ['Stable IDs, versions, permissions, and provenance', 'Only the final answer text', 'Random chunk order', 'The user password'], 0, 'Provenance makes updates, access checks, and citation debugging possible.', ['Right. Identity, version, permission and provenance are what make citation and access control possible later.', 'Discarding provenance makes the answer unauditable, which defeats most of the reason to use retrieval.', 'Order is not the issue; losing the metadata attached to each chunk is.', 'Credentials should never be in a corpus at all.']),
      q('Which is a whole-system RAG metric?', ['Citation accuracy on the final answer', 'Vector dimension alone', 'Number of colors in the UI', 'Raw token vocabulary size'], 0, 'RAG evaluation should cover retrieval, answer support, citation correctness, abstention, latency, and cost.', ['Right. It spans retrieval and generation, which is what makes it a system-level measure.', 'An implementation detail of the index. It tells you nothing about answer quality.', 'Not a measure of anything the system does.', 'A property of the model, fixed before you built anything.']),
    ],
  },
  {
    id: 'agents',
    name: 'Agents and tool use',
    summary: 'Check whether you can design bounded loops with useful tools and safe stopping conditions.',
    questions: [
      q('What makes a system agentic?', ['A model helps choose actions in a bounded observe-and-act loop', 'It has a chat bubble', 'It always uses multiple models', 'It writes a system prompt'], 0, 'An agent combines model-driven action choice with state, tools, and control logic.', ['Right. Model-chosen actions inside a loop with state, tools and control logic around it.', 'The interface is irrelevant. A chat UI over a fixed workflow is still a workflow.', 'One model is enough. Multiple models is an implementation choice, not the definition.', 'Every LLM call has a system prompt. That does not make it an agent.']),
      q('When is a deterministic workflow usually preferable?', ['When steps and rules are known and predictability matters', 'When no success criterion exists', 'When every task is ambiguous', 'When tools are dangerous'], 0, 'Use the least flexible abstraction that meets the need. Deterministic paths are easier to test and govern.', ['Right. If you can write the sequence, writing it is cheaper, more testable and easier to govern.', 'Without a success criterion, an agent has no way to know when to stop — that argues against autonomy, not for it.', 'Genuine ambiguity in the path is the case for an agent, not against.', 'Dangerous tools argue for approval gates. They do not make the control flow deterministic.']),
      q('Which property makes a tool safer for a model to use?', ['Narrow purpose, typed inputs, clear failures, and least privilege', 'A vague description and broad credentials', 'A hidden side effect', 'Unbounded output'], 0, 'Good tool interfaces reduce ambiguity while authorization and validation protect the system.', ['Right. Narrow scope, typed inputs, legible failures and least privilege — each one limits the blast radius.', 'The worst combination available: the model cannot choose it correctly and the damage is unbounded when it does.', 'A hidden side effect is exactly what nobody can review or approve.', 'Unbounded output eats the context window and can hide the part that mattered.']),
      q('Which is an important agent stop condition?', ['A maximum turn or cost budget', 'An instruction to keep trying forever', 'A random delay only', 'A larger font'], 0, 'Budgets, timeouts, repeated-error limits, and explicit success states prevent runaway loops.', ['Right — and note that a turn limit alone does not bound cost, because one turn can carry an enormous context.', 'That is the failure this question is about.', 'A delay slows the loop without ending it.', 'Not a control of any kind.']),
      q('What should an approval request show?', ['The proposed action, relevant inputs, and expected effect', 'Only the model name', 'A hidden chain of thought', 'An unrelated analytics chart'], 0, 'People can make a useful decision when the risk-bearing action and its consequences are visible.', ['Right. The resolved arguments especially — the recipient is the field an attacker would have changed.', 'The model name tells the approver nothing about what is about to happen.', 'Reasoning is not the decision. What matters is the action and its effect.', 'Irrelevant information makes the decision harder, not easier.']),
      q('Why should tool results be treated as untrusted?', ['They may contain malformed data or instructions aimed at the model', 'Tools cannot return text', 'The model never reads results', 'All APIs are malicious'], 0, 'Tool and retrieval outputs can create indirect injection and data-quality risks.', ['Right. A tool result is text from somewhere you do not control, and it lands in the model\'s context.', 'They routinely do return text, which is exactly the risk.', 'The model reads every result — that is how the loop advances.', 'Overstated. The point is that you cannot verify intent, not that every API is hostile.']),
      q('What is a useful agent trace?', ['Inputs, decisions, tool calls, outputs, state, retries, and final status', 'Only the final text', 'Only token count', 'A screenshot with no IDs'], 0, 'Structured, redacted traces let you reproduce whether a failure came from planning, tools, permissions, or state.', ['Right. Enough to reproduce the run and locate whether planning, tools, permissions or state failed.', 'The final text tells you what happened and never why.', 'A cost signal, not a diagnostic one.', 'Without identifiers you cannot correlate it with anything.']),
      q('What is excessive agency?', ['Giving a model more action power than the task needs', 'A model refusing a request', 'A small context window', 'A lexical search result'], 0, 'Keep authority narrow and require confirmation for consequential or irreversible operations.', ['Right. Authority beyond what the task needs, which is what turns an injection into an incident.', 'A refusal is a limit working, not a failure of scope.', 'A resourcing constraint, unrelated to permissions.', 'A retrieval result has no authority attached to it.']),
    ],
  },
  {
    id: 'mcp',
    name: 'Model Context Protocol',
    summary: 'Practice the MCP roles, primitives, and security boundaries that matter in interviews.',
    questions: [
      q('What is MCP?', ['A protocol for connecting AI applications with external context and capabilities', 'A model training algorithm', 'A database engine', 'A prompt style'], 0, 'MCP standardizes an integration boundary around context and actions; it does not replace app authorization.', ['Right. A standard boundary for exposing context and capabilities to any compliant client.', 'Nothing about training. MCP concerns how a running application reaches tools and content.', 'It transports data; it does not store it.', 'It is a protocol, not a way of writing prompts — though prompts are one of its primitives.']),
      q('Which MCP component coordinates the model experience and client connections?', ['The host', 'The resource', 'The prompt', 'The tool result'], 0, 'The host application coordinates clients, model interaction, lifecycle, and user-facing policy.', ['Right. The host owns lifecycle, client connections and user-facing policy.', 'A resource is content the client can read, not a coordinator.', 'A prompt is a reusable template the server offers, not the coordinating layer.', 'A tool result is data returned from an action.']),
      q('Which primitive is generally model-controlled?', ['Tools', 'Prompts', 'Resources', 'User accounts'], 0, 'MCP describes tools as executable functions exposed for model use, while prompts and resources have different control paths.', ['Right. Tools are executable functions exposed for the model to request.', 'Prompts are typically surfaced for a person to choose, not selected autonomously.', 'Resources are read by the client into context rather than invoked by the model.', 'Not a protocol primitive at all.']),
      q('What do MCP resources provide?', ['Structured data or content for context', 'Only destructive actions', 'A new model checkpoint', 'A browser cookie'], 0, 'Resources carry context. Access checks and data minimization still belong to the application.', ['Right. Content the client can pull into context without an action being taken.', 'Resources are for reading. Actions belong in tools.', 'Nothing to do with model weights.', 'Unrelated to the protocol.']),
      q('Why does capability negotiation matter?', ['Each side can use only the features the other announced', 'It makes all tools safe automatically', 'It removes the need for initialization', 'It increases model temperature'], 0, 'Negotiated capabilities let clients and servers evolve while handling partial implementations explicitly.', ['Right. Each side uses only what the other announced, so partial implementations fail explicitly rather than mysteriously.', 'Negotiation is about feature availability, not safety. Permissions are still yours to enforce.', 'Negotiation happens during initialisation; it does not replace it.', 'Unrelated to sampling.']),
      q('What is a good MCP tool boundary?', ['A narrow action with validated arguments and explicit side effects', 'A shell with unrestricted access', 'A tool that hides its permissions', 'An action that never reports failure'], 0, 'The protocol does not remove the need for least privilege, validation, and observable failures.', ['Right. Narrow action, validated arguments, side effects stated plainly.', 'A general shell hands over every permission the process has, and no schema can constrain that.', 'If permissions are hidden, nobody can review what the tool is allowed to do.', 'Silent failure is worse than loud failure: the model treats an empty result as an answer and moves on.']),
      q('Why should an MCP client avoid exposing every tool on every request?', ['Tool descriptions consume context and can confuse selection', 'MCP tools cannot be listed', 'It prevents all interoperability', 'It makes resources private'], 0, 'Selective exposure and compact descriptions protect context quality and improve tool choice.', ['Right. Every schema is re-sent each turn — a standing cost, and more options make selection worse.', 'They can be listed; that is the mechanism creating the cost.', 'Selective exposure is a client-side choice and does not affect protocol compatibility.', 'Resource privacy is a separate concern from tool exposure.']),
      q('What is the first security question for an MCP server?', ['Who can connect and what data or actions are permitted?', 'Which font is installed?', 'How many tokens were in pretraining?', 'Whether the UI is centered'], 0, 'Map identities, data flows, side effects, consent, updates, and containment before connecting powerful capabilities.', ['Right. Identity and permitted scope come before any capability is connected.', 'Not a security property.', 'A model-training detail with no bearing on server security.', 'A presentation concern.']),
    ],
  },
  {
    id: 'evals',
    name: 'Evals and red teaming',
    summary: 'Test the release discipline behind representative datasets, rubrics, and adversarial cases.',
    questions: [
      q('What is an evaluation?', ['A repeatable comparison against a task and success criterion', 'A single favorable demo', 'A model temperature', 'A production secret'], 0, 'Evals make behavior comparable. The target is usually the whole system configuration, not just the base model.', ['Right. Repeatable, against a stated criterion — which is what makes two runs comparable.', 'One good demo is a selection effect, not a measurement.', 'A parameter, not a measurement.', 'Unrelated to measuring behaviour.']),
      q('What belongs in a golden dataset?', ['Representative examples, criteria, metadata, and edge cases', 'Only easy successes', 'Only synthetic prompts', 'User passwords'], 0, 'A useful set includes ordinary, ambiguous, long, malformed, empty, and security-sensitive inputs where relevant.', ['Right — and the highest-value rows are the failures you have already been reported once.', 'A set of easy cases will keep passing while quality falls on everything else.', 'Synthetic-only data drifts away from how people actually phrase things.', 'Never put credentials in a dataset.']),
      q('When is exact match a poor sole metric?', ['When quality includes nuance, evidence, or helpful partial credit', 'When outputs are always fixed IDs', 'When latency is irrelevant', 'Never'], 0, 'Use rubrics or additional checks for nuanced behavior, and inspect examples alongside aggregate scores.', ['Right. Where partial credit and evidence matter, a binary string comparison discards the signal.', 'Fixed identifiers are the case where exact match works well.', 'Latency is a separate axis; it does not affect whether exact match measures quality.', 'There are clear cases where it is the wrong tool, which is what this question is about.']),
      q('How should an LLM judge be used responsibly?', ['Calibrate it against human labels and audit its biases', 'Treat every judge score as ground truth', 'Use a different prompt every run', 'Hide the rubric'], 0, 'Model judges can scale review but may favor verbosity or share blind spots with the system under test.', ['Right. Compare it against human grades on a sample before you trust the number, and know its biases.', 'A judge is a model with the same failure modes as the one under test, including sharing its blind spots.', 'Changing the prompt each run makes scores incomparable between runs.', 'A hidden rubric cannot be reviewed, so disagreement cannot be diagnosed.']),
      q('What does red teaming seek?', ['Adversarial paths that violate intended behavior or security', 'Only higher marketing conversion', 'A shorter prompt', 'A new embedding dimension'], 0, 'Red teaming targets injection, leakage, unsafe actions, misuse, bias, and other failure paths.', ['Right. The paths that violate intended behaviour, found deliberately rather than in production.', 'A different discipline entirely.', 'Brevity is not a security property.', 'An implementation detail, unrelated to adversarial testing.']),
      q('Why keep a held-out test split?', ['To reduce overfitting decisions to the cases used during tuning', 'To make retrieval faster', 'To increase temperature', 'To avoid recording failures'], 0, 'An untouched test set provides a more honest release signal after iteration.', ['Right. Tuning against a set turns it into training data, and its score stops predicting anything.', 'Splits have no effect on retrieval speed.', 'Unrelated to sampling.', 'The opposite: failures should be recorded and turned into cases.']),
      q('What should a release eval track besides quality?', ['Latency, cost, safety, and important failure rates', 'Only word count', 'Only UI color', 'Nothing else'], 0, 'A system can improve answer quality while becoming too slow, expensive, or risky to ship.', ['Right. A system can get better answers while becoming too slow, too costly or too risky to ship.', 'Length is not a release criterion.', 'Not a property of the system\'s behaviour.', 'Quality alone hides the trade you just made.']),
      q('What should happen to a serious production failure?', ['Turn it into a reproducible regression test after mitigation', 'Delete the trace', 'Hide it from the dataset', 'Increase randomness'], 0, 'Failures become valuable when they are reproducible, assigned a mitigation, and checked in future releases.', ['Right. A failure becomes valuable the moment it is reproducible and checked on every future release.', 'Deleting the trace destroys the only record of what happened.', 'Hiding it guarantees the same failure returns.', 'Randomness makes reproduction harder, which is the opposite of what you need.']),
    ],
  },
];

export const getQuizTrack = (id: string) => quizTracks.find((track) => track.id === id);
