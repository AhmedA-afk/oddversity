# Expected artifacts

- `predictions.csv` with `id,prediction` for every fixture row.
- `metrics.json` with `n`, `precision`, and `recall`; acceptance requires recall >= 0.75 and precision >= 0.60.
- For a real-data submission: time-aware split evidence, a statement on duration leakage, and expected campaign cost/value.

