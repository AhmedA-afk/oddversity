# Expected artifacts

- `triage.csv` with an `inspect` decision for every fixture row.
- `metrics.json` with `n`, `recall`, and `precision`; fixture acceptance requires recall >= 0.75 and precision >= 0.60.
- For real data: missing-value policy, PR/expected-cost threshold analysis, validation split, and human inspection escalation policy.

