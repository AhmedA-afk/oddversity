# Transformer attention lab

Implement one deterministic attention head with an explicit mask. This isolates score scaling, stable normalization, and masking semantics.

```sh
python3 test_public.py
STUDENT_MODULE=reference_solution python3 test_public.py
```

Finish the TODOs in `starter.py`; no package downloads are needed.
