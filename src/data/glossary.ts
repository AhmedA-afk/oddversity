export interface Term {
  term: string;
  short: string;
  body: string;
  group: 'Models & runtime' | 'Prompting & context' | 'Retrieval' | 'Agents & tools' | 'Training & tuning' | 'Evaluation & safety' | 'Operations';
  see?: string;
}

// Plain-language definitions, written to be correct rather than impressive.
// `short` is the one-line answer; `body` is the part that stops the term being
// misused. `see` links to the lesson that goes properly into it.
export const terms: Term[] = [
  {
    term: 'Token',
    group: 'Models & runtime',
    short: 'The unit a model actually reads and writes — roughly a word fragment, not a word.',
    body: 'Text is split into tokens before a model sees it. Common English words are usually one token; rare words, names, code and non-Latin scripts split into several. Tokens are what you are billed for, what the context window is measured in, and what generation latency scales with — which is why "how long is this prompt" is a question with a numeric answer, not a vibe.',
    see: '/learn/llm-foundations/tokenization-explained',
  },
  {
    term: 'Context window',
    group: 'Models & runtime',
    short: 'The maximum number of tokens a model can consider in one request, input and output combined.',
    body: 'It is a hard budget per request, not memory. Nothing persists between calls unless you send it again. Quality also degrades unevenly across a long window — material in the middle is attended to less reliably than material at either end — so a large window is a ceiling, not a recommendation.',
    see: '/learn/context-engineering/context-window-anatomy',
  },
  {
    term: 'Temperature',
    group: 'Models & runtime',
    short: 'A sampling parameter controlling how sharply the model favours its highest-probability next token.',
    body: 'At 0 the model takes the most likely token every time, which is repeatable but not guaranteed identical across runs or infrastructure. Higher values flatten the distribution and produce more varied output. Temperature changes selection, not knowledge — turning it down does not make a model more accurate, only more consistent.',
    see: '/learn/llm-foundations/sampling-temperature-top-p',
  },
  {
    term: 'Inference',
    group: 'Models & runtime',
    short: 'Running a trained model to get an output. The parameters do not change.',
    body: 'Everything you do at runtime — prompting, retrieval, tool use, few-shot examples — changes the input, never the weights. This is the distinction that makes "the model learned from our conversation" false for a standard API call.',
  },
  {
    term: 'Embedding',
    group: 'Retrieval',
    short: 'A vector of numbers representing a piece of content, positioned so that similar content sits nearby.',
    body: 'Embeddings let you search by meaning instead of by exact words. "Similar" is defined by the model that produced them, so two texts can be close in one embedding space and far apart in another. You cannot mix vectors from different models, and changing embedding model means reindexing everything.',
    see: '/learn/rag/embeddings-and-semantic-similarity',
  },
  {
    term: 'Cosine similarity',
    group: 'Retrieval',
    short: 'A score from -1 to 1 measuring the angle between two vectors, ignoring their length.',
    body: 'The default relevance measure in vector search. With normalised vectors it is just a dot product. A high score means "points in a similar direction in this model\'s space" — which correlates with relevance but is not the same thing, and the absolute value is not comparable across models.',
    see: '/learn/maths-foundations/cosine-similarity',
  },
  {
    term: 'RAG',
    group: 'Retrieval',
    short: 'Retrieval-augmented generation: fetch relevant passages, put them in the prompt, answer from them.',
    body: 'RAG addresses knowledge the model does not have — private, recent, or too large for the context window — and gives you a source to cite. It does not make a model truthful. If retrieval returns the wrong passage, the answer is wrong with full confidence, which is why the retrieval half deserves most of your attention.',
    see: '/learn/rag/what-is-rag-and-when-to-use-it',
  },
  {
    term: 'Chunking',
    group: 'Retrieval',
    short: 'Splitting documents into retrievable pieces before embedding them.',
    body: 'The boundary decision matters more than the size. Cut a table from its headers or a step from its procedure and the chunk retrieves without meaning what it needs to mean. Split on the document\'s own structure first — headings, paragraphs, function definitions — and treat size as a fallback.',
    see: '/learn/rag/chunking-strategies-for-documents',
  },
  {
    term: 'Hybrid search',
    group: 'Retrieval',
    short: 'Running keyword and vector search together and merging the rankings.',
    body: 'Dense vectors are strong on meaning and weak on rare exact tokens — product codes, error strings, version numbers, surnames. Keyword search is the reverse. Merging them, usually with reciprocal rank fusion, is the single highest-value upgrade for most real corpora.',
    see: '/learn/rag/hybrid-search-lexical-and-vector',
  },
  {
    term: 'Reranking',
    group: 'Retrieval',
    short: 'A second, more expensive model reorders the first search\'s candidates.',
    body: 'Retrieve thirty cheaply, then have a cross-encoder score each candidate against the query and keep the best five. It costs latency and buys precision, and it is usually a better use of context budget than sending more chunks.',
    see: '/learn/rag/reranking-retrieved-results',
  },
  {
    term: 'Vector database',
    group: 'Retrieval',
    short: 'A store optimised for nearest-neighbour search over embeddings.',
    body: 'Useful once your corpus stops fitting comfortably in memory or you need filtering, replication and incremental updates. It is a scale decision, not a quality one — SQLite plus NumPy retrieves exactly as well at small scale.',
    see: '/learn/rag/choosing-a-vector-database',
  },
  {
    term: 'ANN index',
    group: 'Retrieval',
    short: 'Approximate nearest neighbour — a structure that finds near-best matches quickly instead of best matches slowly.',
    body: 'HNSW, IVF and similar indexes trade a small amount of recall for a large speed gain. The trade is tunable, and the default settings of your vector store are a choice someone made for you.',
    see: '/learn/rag/similarity-search-and-ann-indexes',
  },
  {
    term: 'Prompt engineering',
    group: 'Prompting & context',
    short: 'Designing the text of a request so a model reliably produces the output you need.',
    body: 'Mostly unglamorous work: state the task, give examples, specify the format, name the failure you keep seeing. It is the cheapest lever available and the first one to exhaust before reaching for retrieval or fine-tuning.',
    see: '/learn/prompt-engineering/what-prompt-engineering-is',
  },
  {
    term: 'Context engineering',
    group: 'Prompting & context',
    short: 'Deciding what goes into the context window, in what order, and what gets left out.',
    body: 'The step up from prompt engineering once your system assembles context from several sources — history, retrieved documents, tool results, instructions. The discipline is subtractive: most context problems are too much irrelevant material, not too little.',
    see: '/learn/context-engineering/context-engineering-vs-prompting',
  },
  {
    term: 'Few-shot prompting',
    group: 'Prompting & context',
    short: 'Including examples of correct input-output pairs in the prompt.',
    body: 'Often does most of what people expect fine-tuning to do, at zero training cost. Three good examples usually beat a paragraph of description, and examples that cover the edge cases beat examples that cover the obvious ones.',
    see: '/learn/prompt-engineering/few-shot-prompting',
  },
  {
    term: 'Chain of thought',
    group: 'Prompting & context',
    short: 'Prompting a model to write out intermediate reasoning before its answer.',
    body: 'Helps on multi-step problems by giving the model tokens to compute in. The written steps are a useful artifact but not a faithful record — a model can reach the right answer via reasoning it did not print, or print reasoning that does not support its answer.',
    see: '/learn/prompt-engineering/chain-of-thought-prompting',
  },
  {
    term: 'System prompt',
    group: 'Prompting & context',
    short: 'Instructions given a privileged position in the conversation, setting persistent behaviour.',
    body: 'Strong influence, not a security boundary. Content the model reads later can and does override it. Use it for behaviour and tone; enforce permissions in code.',
  },
  {
    term: 'Prompt caching',
    group: 'Operations',
    short: 'Reusing the provider\'s computed state for an identical prompt prefix across requests.',
    body: 'Cached input tokens are billed at a steep discount. It works on an exact prefix, so order your prompt stable-first — system instructions, tools, reference documents — and put anything variable last. A timestamp near the top silently disables it.',
    see: '/learn/production/prompt-and-semantic-caching',
  },
  {
    term: 'Structured output',
    group: 'Prompting & context',
    short: 'Constraining a model to produce output matching a schema.',
    body: 'Modern providers restrict decoding so invalid JSON cannot be produced, which is categorically stronger than asking for JSON in the prompt. It guarantees shape, never content: a well-typed, confidently wrong value passes every schema check.',
    see: '/learn/structured-outputs/why-structured-output',
  },
  {
    term: 'Tool calling',
    group: 'Agents & tools',
    short: 'The model returns a structured request to run one of your functions; your code runs it and returns the result.',
    body: 'Also called function calling. The model never executes anything — it proposes, and your code decides whether to comply. That gap is where every permission check belongs.',
    see: '/learn/tools-function-calling/what-is-tool-calling',
  },
  {
    term: 'Agent',
    group: 'Agents & tools',
    short: 'A system where a model chooses which actions to take, in a loop, until a goal is met.',
    body: 'The distinction from a workflow is who picks the next step. If you wrote the sequence, it is a workflow — usually cheaper, more predictable and easier to debug. Agents earn their cost when the path genuinely cannot be known in advance.',
    see: '/learn/agentic-ai/agents-vs-workflows',
  },
  {
    term: 'Agent loop',
    group: 'Agents & tools',
    short: 'Call the model, execute any requested tools, append the results, repeat until it stops asking.',
    body: 'About forty lines of code. Every framework is this plus retries, tracing, state and orchestration. It needs three stopping conditions — turn limit, token budget, and a no-progress detector — because only the first is obvious and only the last catches a stuck agent.',
    see: '/learn/agentic-ai/the-agent-loop',
  },
  {
    term: 'ReAct',
    group: 'Agents & tools',
    short: 'A pattern that interleaves reasoning and acting: think, act, observe, repeat.',
    body: 'The shape underneath most tool-using agents. The value is that the observation from each action feeds the next decision, so the agent can recover from a surprise rather than following a plan off a cliff.',
    see: '/learn/agentic-ai/react-pattern',
  },
  {
    term: 'MCP',
    group: 'Agents & tools',
    short: 'Model Context Protocol — an open standard for exposing tools, resources and prompts to any AI client.',
    body: 'Solves the M×N integration problem: without a standard, every assistant needs a bespoke connector to every tool. A server exposes capabilities; any compliant client can use them. Servers commonly run locally over stdio, which is why writing to stdout corrupts the connection.',
    see: '/learn/mcp/what-is-mcp',
  },
  {
    term: 'Multi-agent system',
    group: 'Agents & tools',
    short: 'Several agents with distinct roles or contexts working on one problem.',
    body: 'Genuinely useful for context isolation — a subagent can burn its own window on a research task and return a summary. It also multiplies cost, latency and failure modes, so the honest default is one agent until you can name the specific reason for a second.',
    see: '/learn/agentic-ai/multi-agent-patterns',
  },
  {
    term: 'Harness',
    group: 'Agents & tools',
    short: 'The application layer around a model: state, permissions, budgets, tool routing, approvals, observability.',
    body: 'The model is the interesting part and the harness is the part that makes it operable. Most production quality problems are harness problems, and most of them are unglamorous — a missing timeout, an unbounded retry, a tool with too much authority.',
    see: '/learn/harness-design/what-is-a-harness',
  },
  {
    term: 'Prompt injection',
    group: 'Evaluation & safety',
    short: 'Text the model reads changes what the model does.',
    body: 'Direct injection comes from the user; indirect comes from a document, web page or tool result they asked about — and indirect is the dangerous one, because the request looks legitimate. There is no reliable way to make a model separate instructions from data, so the defence is architectural: narrow tools, server-side permissions, and a human on anything irreversible.',
    see: '/learn/evals-red-teaming/prompt-injection-basics',
  },
  {
    term: 'Jailbreak',
    group: 'Evaluation & safety',
    short: 'Getting a model to produce output its training and system instructions were meant to prevent.',
    body: 'Distinct from prompt injection, though the techniques overlap. Jailbreaking targets the model\'s policy; injection targets your application\'s control flow. Your app can be perfectly injection-resistant and still jailbreakable, and vice versa.',
    see: '/learn/evals-red-teaming/jailbreak-techniques-and-defenses',
  },
  {
    term: 'Hallucination',
    group: 'Evaluation & safety',
    short: 'Fluent, confident output that is not supported by the input or by fact.',
    body: 'Not a bug to be patched but a consequence of how generation works: the model produces the most plausible continuation, and plausible is not the same as true. Retrieval and citation requirements reduce it; nothing eliminates it, so systems that matter need a verification step rather than trust.',
    see: '/learn/hallucinations/what-is-a-hallucination',
  },
  {
    term: 'Eval',
    group: 'Evaluation & safety',
    short: 'A dataset of inputs with a grader that decides whether an output is acceptable.',
    body: 'The thing that turns "this feels better" into a number that moves. Twenty rows and a deterministic grader beats an elaborate framework you never populate. Grade the grader before you trust the score.',
    see: '/learn/evals-red-teaming/why-evals-matter',
  },
  {
    term: 'Golden dataset',
    group: 'Evaluation & safety',
    short: 'Curated examples with known-correct outputs, used as the reference for every change.',
    body: 'Its value comes from being stable and from containing your real past failures. Every bug report is a row worth adding, because it is a regression you have already paid for once.',
    see: '/learn/evals-red-teaming/building-a-golden-dataset',
  },
  {
    term: 'LLM-as-judge',
    group: 'Evaluation & safety',
    short: 'Using a model to grade another model\'s output against a rubric.',
    body: 'Necessary for qualities a string match cannot reach, and biased in known ways: it favours longer answers, answers in its own style, and fluent-but-wrong over terse-but-right. Calibrate it against human grades on a sample before you rely on the number, and prefer a binary verdict to a 1–5 score.',
    see: '/learn/evals-red-teaming/llm-judge-bias-and-calibration',
  },
  {
    term: 'Red teaming',
    group: 'Evaluation & safety',
    short: 'Deliberately attacking your own system to find the failures before someone else does.',
    body: 'For AI systems this means adversarial inputs, injection attempts, scope-escape probes and tool-abuse tests, run continuously rather than once. The output worth tracking is a break rate over time, because it will regress when the model underneath you changes.',
    see: '/learn/evals-red-teaming/adversarial-red-teaming-process',
  },
  {
    term: 'Guardrails',
    group: 'Evaluation & safety',
    short: 'Checks applied to input or output independently of the model.',
    body: 'PII detection, topic filters, schema validation, allowlists. They work because they do not depend on the model cooperating — which is exactly why an instruction in a system prompt is not a guardrail.',
    see: '/learn/harness-design/guardrails-as-code',
  },
  {
    term: 'Fine-tuning',
    group: 'Training & tuning',
    short: 'Continuing training on your own examples to change a model\'s default behaviour.',
    body: 'Teaches form far more reliably than fact. Good for a consistent style, a rigid output shape, or a narrow classification task at volume. Poor for knowledge that changes, and structurally unable to enforce per-user permissions — which is why so many fine-tuning projects should have been retrieval projects.',
    see: '/learn/fine-tuning/fine-tune-vs-prompt-vs-rag',
  },
  {
    term: 'LoRA',
    group: 'Training & tuning',
    short: 'Low-rank adaptation — training a small set of extra parameters instead of the whole model.',
    body: 'Dramatically cheaper than full fine-tuning in memory and time, and the resulting adapter is small enough to swap at serving time. QLoRA adds quantisation of the base model to cut memory further. For most applied work this is what "fine-tuning" now means in practice.',
    see: '/learn/fine-tuning/lora-and-qlora-fine-tuning',
  },
  {
    term: 'RLHF',
    group: 'Training & tuning',
    short: 'Reinforcement learning from human feedback — training a model against human preferences between outputs.',
    body: 'The step that turns a raw pretrained model into something helpful and well-behaved. Preference-optimisation methods such as DPO reach similar ends with a simpler pipeline and no separate reward model.',
    see: '/learn/fine-tuning/rlhf-reward-modeling-and-ppo',
  },
  {
    term: 'Distillation',
    group: 'Training & tuning',
    short: 'Training a small model on a large model\'s outputs.',
    body: 'A practical route to cheap, fast inference on a narrow task: use the strong model to label data, train the small one, evaluate honestly. The small model inherits the large one\'s mistakes as well as its skills.',
    see: '/learn/fine-tuning/knowledge-distillation',
  },
  {
    term: 'Quantisation',
    group: 'Training & tuning',
    short: 'Storing model weights at lower numeric precision to cut memory and increase speed.',
    body: 'The main lever for running larger models on smaller hardware. Quality loss is usually modest at 8-bit and task-dependent below it — so it is a decision to make with an eval set open, not a default to accept.',
    see: '/learn/fine-tuning/quantization-gguf-awq-gptq',
  },
  {
    term: 'Observability',
    group: 'Operations',
    short: 'Being able to reconstruct what happened in a specific request after the fact.',
    body: 'For AI systems this means logging the request ID, model version, prompt version, token counts, latency, stop reason and whether a fallback fired. The stop reason is the field people omit and then need — a completion truncated at the token limit looks like a short answer in the database and like a bug to the user.',
    see: '/learn/production/observability-cost-and-latency',
  },
  {
    term: 'Model routing',
    group: 'Operations',
    short: 'Sending different requests to different models based on difficulty, cost or latency.',
    body: 'Classification, extraction and short rewrites rarely need your largest model. Move one request type down a tier, run your eval, keep the change if the score holds — a twenty-minute experiment with a permanent payoff.',
    see: '/learn/production/model-routing-by-task-complexity',
  },
  {
    term: 'Graceful degradation',
    group: 'Operations',
    short: 'A defined, useful behaviour when the model is unavailable.',
    body: 'Keyword search instead of semantic search; a template instead of a generated summary; a clearly marked cached result. The unacceptable default is a spinner that never resolves — and it is the default if nobody chooses.',
    see: '/learn/production/load-shedding-and-graceful-degradation',
  },
  {
    term: 'Streaming',
    group: 'Operations',
    short: 'Returning tokens as they are generated instead of waiting for the complete response.',
    body: 'Does not make anything faster; makes it feel faster, which is most of the complaint. It complicates validation, because you cannot check a schema against a partial object — so structured output and streaming need deliberate handling together.',
    see: '/learn/production/observability-cost-and-latency',
  },
  {
    term: 'Batch API',
    group: 'Operations',
    short: 'Asynchronous, high-latency, lower-cost processing of many requests at once.',
    body: 'Meaningfully cheaper per token at every major provider. Free money for any workload that does not need an answer in the next few seconds — nightly classification, backfills, bulk enrichment.',
    see: '/learn/production/batch-api-for-non-interactive-workloads',
  },
];

export const glossaryGroups = [
  'Models & runtime',
  'Prompting & context',
  'Retrieval',
  'Agents & tools',
  'Training & tuning',
  'Evaluation & safety',
  'Operations',
] as const;

export const slugForTerm = (term: string) =>
  term.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '');
