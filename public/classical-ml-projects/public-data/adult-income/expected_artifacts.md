# Expected artifacts

- `predictions.csv`: one prediction for every fixture `id`, with no label column.
- `metrics.json`: valid JSON containing `n` and `accuracy`; fixture accuracy must be at least 0.75.
- For the real dataset: add a model card with provenance, split design, metrics by sex/race where lawful and appropriate, limitations, and threshold rationale.

