# Generative toy lab

Use a small deterministic fixture to implement two generative-model building blocks: VAE reparameterization and a discriminator loss. No generative images or external frameworks are required.

```sh
python3 test_public.py
STUDENT_MODULE=reference_solution python3 test_public.py
```

Implement the TODOs in `starter.py` and write the requested short interpretation.
